version: "3"
services:
  clamav:
    container_name: clamav
    image: mkodockx/docker-clamav
    restart: always
  app:
    image: app:latest
    container_name: clamav-scan-api
    restart: always
    build: .
    ports:
      - "0:3000"
    environment:
      - CLAMAV_HOST=clamav
      - CLAMAV_PORT=3310
    links:
      - clamav
    depends_on:
      - clamav
